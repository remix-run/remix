# style-server demo

Minimal watch-mode demo for `remix/style-server`.

It serves CSS files from source, keeps `@import` relationships live, and lets you edit styles without restarting the server.

## Run

```sh
pnpm -C demos/style-server dev
```

Then open [http://localhost:44100](http://localhost:44100).
