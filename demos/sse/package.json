{
  "name": "sse-demo",
  "private": true,
  "type": "module",
  "dependencies": {
    "@remix-run/component": "workspace:*",
    "@remix-run/events": "jam",
    "remix": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "esbuild": "^0.25.10",
    "tsx": "catalog:"
  },
  "scripts": {
    "dev": "pnpm run --filter \".\" --parallel \"/^dev:/\"",
    "dev:server": "NODE_ENV=development tsx watch server.js",
    "dev:browser": "esbuild app/assets/*.js --outbase=app/assets --outdir=public/assets --bundle --minify --splitting --format=esm --entry-names='[dir]/[name]' --chunk-names='chunks/[name]-[hash]' --sourcemap --watch",
    "start": "tsx server.js",
    "test": "tsx --test",
    "typecheck": "tsgo --noEmit"
  }
}
