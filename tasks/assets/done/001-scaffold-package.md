### Scaffold `assets-middleware` package

Create the package structure with standard Remix monorepo conventions.

**Acceptance Criteria:**

- [x] `packages/assets-middleware/` directory exists
- [x] `package.json` with correct name, exports, dependencies
- [x] `tsconfig.json` and `tsconfig.build.json`
- [x] `src/index.ts` with placeholder export
- [x] Package builds successfully with `pnpm --filter @remix-run/assets-middleware run build`
