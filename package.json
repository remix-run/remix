{
  "name": "fetch-super-headers",
  "version": "0.1.0",
  "description": "A suite of tools that make it a little more fun to work with fetch Headers",
  "main": "dist/index.js",
  "scripts": {
    "build": "pnpm run build:cjs && pnpm run build:esm",
    "build:cjs": "tsc --outDir dist --project tsconfig.lib.json",
    "build:esm": "esbuild src/index.ts --bundle --format=esm --target=node16 --outfile=dist/index.mjs",
    "test": "node --import tsimp/import --test ./src/**/*.spec.ts"
  },
  "author": "Michael Jackson <mjijackson@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.10",
    "esbuild": "^0.23.0",
    "tsimp": "^2.0.11",
    "typescript": "^5.5.3"
  }
}
