name: 🧪 Test

on:
  push:
    branches:
      - main
      - dev
    paths-ignore:
      - "docs/**"
      - "scripts/**"
      - "**/README.md"
  pull_request:
    paths-ignore:
      - "docs/**"
      - "**/*.md"

jobs:
  test:
    if: github.repository == 'remix-run/remix'
    uses: jenseng/remix/.github/workflows/reusable-test.yml@npm-install-cache-test
    with:
      node_version: '["latest"]'
