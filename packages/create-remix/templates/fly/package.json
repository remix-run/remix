{
  "scripts": {
    "postinstall": "remix setup node",
    "build": "remix build",
    "dev": "remix dev",
    "deploy": "rm -rf build && flyctl deploy",
    "start": "remix-serve build"
  },
  "dependencies": {
    "@remix-run/serve": "*"
  }
}
