{
  "scripts": {
    "postinstall": "remix setup node",
    "build": "remix build",
    "dev": "remix dev",
    "deploy": "npm run build && fly deploy --remote-only",
    "start": "remix-serve build"
  },
  "dependencies": {
    "@remix-run/serve": "*"
  }
}
