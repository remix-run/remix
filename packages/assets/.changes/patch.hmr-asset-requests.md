Improve browser HMR when changes affect multiple nested or shared dependency paths, when several edits arrive in one or successive batches, and when a module adds or removes a self-accept handler. Dynamically imported modules now join the active HMR graph once loaded, and newly requested entries retain cache-busting URLs for dependencies updated earlier in the session. HMR also recovers cleanly as imports are added, retargeted, or removed, preserves the changed source paths when coordinating browser and server updates, and supports bare imports from a configured browser HMR module importer. Import maps remain stable across cache-busting updates, avoiding unnecessary page reloads after repeated edits.
