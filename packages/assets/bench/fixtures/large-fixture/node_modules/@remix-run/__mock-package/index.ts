import { renderMockOptional } from '@remix-run/__mock-optional'
import { renderMockPanel } from '@remix-run/__mock-ui'
import { formatMockLabel } from '@remix-run/__mock-utils'

export function renderMockPackage(): string {
  return renderMockPanel(formatMockLabel('mock package'), renderMockOptional())
}
