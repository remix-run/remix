import { mockBorder, mockSpacing } from '@remix-run/__mock-tokens'

export function renderMockPanel(label: string, detail: string): string {
  return `<section style="border:${mockBorder};padding:${mockSpacing}">${label}:${detail}</section>`
}
