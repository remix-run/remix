BREAKING CHANGE: Remove the built-in Atmosphere auth provider and its provider-specific DPoP token model from `remix/auth`. Use `OAuthTokens` in place of `OAuthStandardTokens` and remove the third type argument from shared OAuth types. Applications using the Atmosphere provider must remove it or move their atproto authentication to a separate integration.
