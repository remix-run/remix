`remix/ui/server` can now render under a Content Security Policy that names a nonce. Pass `nonce` to `renderToStream()` or `renderToString()` and it is stamped on the import map script and the server-rendered `<style>` tags the framework emits. `renderToString()` also accepts the other rendering options supported by `renderToStream()` (see #11926).
