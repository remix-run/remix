BREAKING CHANGE: Added static JSONC configuration through `remix.json` and the global `remix --config <path>` option, initially for `remix test`. Executable `remix-test.config.ts` and `.js` files are no longer discovered; move test settings under `remix.json#test` (see #11628).
