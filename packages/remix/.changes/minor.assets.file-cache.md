Customize transformed-file caching in `remix/assets` with the exported `FileCache` interface and its `get` and `put` methods. Existing `FileStorage` configurations remain supported. Set `files.cache: true` to opt into a bounded disk cache. Caching remains disabled when the option is omitted or `false` (see #11859).
