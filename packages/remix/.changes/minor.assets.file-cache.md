Customize transformed-file caching in `remix/assets` with the exported `FileCache` interface and its `get` and `put` methods. Existing `FileStorage` configurations remain supported. Omitting `files.cache` now uses a bounded disk cache, and `files.cache: false` disables caching (see #11859).
