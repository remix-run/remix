Improve browser HMR in `remix/assets` so cache-busting URLs propagate through affected nested and shared dependency paths, dynamically imported modules join HMR tracking once loaded, and entries loaded later use current URLs for dependencies updated earlier in the session. HMR now also handles successive edit batches, self-accept handler changes, and imports being added, retargeted, or removed without stale graph state or avoidable full-page reloads.
