Added `remix db` commands for wiping, migrating, inspecting, seeding, and resetting a database configured statically in `remix.json`. Built-in SQLite, PostgreSQL, and MySQL adapters and explicit custom database factories are supported. Command flags override configured values, and destructive commands require `--force` (see #11608, #11639).
