Simplify the Remix skill bundled with new apps into a README-first guide with focused app-building recipes and a `remix/*` package index. Correct rendering and typed-context examples, add guidance for CSRF-protected forms, upload lifecycles, and preserving validation errors during enhanced navigation.

Scaffolded apps now run `remix test` from `npm test` and include a router smoke test at `app/actions/controller.test.ts`.
