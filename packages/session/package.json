{
  "name": "@remix-run/session",
  "version": "0.1.0",
  "description": "A toolkit for working with sessions in JavaScript",
  "author": "Remix Software <hello@remix.run>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/remix-run/remix.git",
    "directory": "packages/session"
  },
  "homepage": "https://github.com/remix-run/remix/tree/main/packages/session#readme",
  "files": [
    "LICENSE",
    "README.md",
    "dist",
    "src",
    "!src/**/*.test.ts"
  ],
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./cookie-storage": "./src/cookie-storage.ts",
    "./file-storage": "./src/file-storage.ts",
    "./memory-storage": "./src/memory-storage.ts",
    "./package.json": "./package.json"
  },
  "publishConfig": {
    "exports": {
      ".": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "./cookie-storage": {
        "types": "./dist/cookie-storage.d.ts",
        "default": "./dist/cookie-storage.js"
      },
      "./file-storage": {
        "types": "./dist/file-storage.d.ts",
        "default": "./dist/file-storage.js"
      },
      "./memory-storage": {
        "types": "./dist/memory-storage.d.ts",
        "default": "./dist/memory-storage.js"
      },
      "./package.json": "./package.json"
    }
  },
  "dependencies": {
    "@remix-run/cookie": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "esbuild": "^0.25.10"
  },
  "scripts": {
    "build": "pnpm run clean && pnpm run build:types && pnpm run build:index && pnpm run build:cookie-storage && pnpm run build:file-storage && pnpm run build:memory-storage",
    "build:index": "esbuild src/index.ts --bundle --external:@remix-run/cookie --outfile=dist/index.js --format=esm --platform=neutral --sourcemap",
    "build:cookie-storage": "esbuild src/cookie-storage.ts --bundle --external:@remix-run/session --outfile=dist/cookie-storage.js --format=esm --platform=neutral --sourcemap",
    "build:file-storage": "esbuild src/file-storage.ts --bundle --external:@remix-run/session --outfile=dist/file-storage.js --format=esm --platform=node --sourcemap",
    "build:memory-storage": "esbuild src/memory-storage.ts --bundle --external:@remix-run/session --outfile=dist/memory-storage.js --format=esm --platform=neutral --sourcemap",
    "build:types": "tsc --project tsconfig.build.json",
    "clean": "rm -rf dist",
    "prepublishOnly": "pnpm run build",
    "test": "node --disable-warning=ExperimentalWarning --test './src/**/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "http",
    "session",
    "sessions",
    "http-sessions",
    "cookie",
    "cookies"
  ]
}
