Added `ui.row` and `ui.stack` layout recipes to `@remix-run/theme`, along with alignment variants for common horizontal and vertical arrangements.

Updated the `theme-rmx-01` demo to document and showcase the new layout recipes on a dedicated UI recipe page.
