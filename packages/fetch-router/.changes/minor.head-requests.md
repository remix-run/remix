Routes registered for `GET` now serve `HEAD` requests automatically. The `GET` handler runs as usual and the router strips the body from its response, so `HEAD` requests observe the same status and headers as the equivalent `GET` request. Previously a `HEAD` request only matched routes explicitly registered for `HEAD` or `ANY`. An explicit `HEAD` route for the same pattern still takes precedence over the `GET` route.
