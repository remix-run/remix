Add client-side hydration support with `createHydrationRoot` and `hydrate` exports.

The `hydrate()` function wraps components for selective hydration, preserving server-rendered HTML while enabling interactivity. Also exports `createRangeRoot` and `createScheduler` utilities.
