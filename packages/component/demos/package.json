{
  "name": "component-demos",
  "private": true,
  "type": "module",
  "dependencies": {
    "@remix-run/component": "workspace:^",
    "@remix-run/interaction": "workspace:^"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "esbuild": "^0.25.11",
    "serve": "^14.2.5"
  },
  "scripts": {
    "start": "concurrently \"pnpm watch\" \"npx serve .\"",
    "watch": "esbuild ./*/entry.tsx --bundle --outdir=. --out-extension:.js=.bundled.js --format=esm --platform=browser --target=es2020 --sourcemap=inline --watch"
  }
}
