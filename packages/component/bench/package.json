{
  "private": true,
  "type": "module",
  "devDependencies": {
    "@types/node": "catalog:",
    "playwright": "^1.49.1",
    "tsx": "catalog:"
  },
  "scripts": {
    "baseline": "node baseline.ts",
    "start": "tsx server.ts",
    "dev": "tsx watch server.ts",
    "build-frameworks": "pnpm --filter \"component-benchmark-*\" run build",
    "bench": "tsx runner.ts",
    "bench:remix": "tsx runner.ts --framework remix --cpu 4 --warmups 5 --runs 20 --headless"
  }
}
