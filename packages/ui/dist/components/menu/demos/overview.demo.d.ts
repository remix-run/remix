import { type Handle } from '@remix-run/ui';
/**
 * @name Menu Overview
 * @description A hierarchical menu with checkboxes, radio groups, submenus, and separators.
 * @layout center
 */
export default function Example(handle: Handle): () => import("@remix-run/ui").RemixElement;
