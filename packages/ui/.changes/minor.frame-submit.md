Submit a form or `FormData` directly to a frame with `frame.submit(options)`. The API uses one resolver request to submit and render the response in the frame without changing browser history. It accepts an optional action, method, encoding, submitter, and abort signal. A newer submit or reload cancels earlier client work for that frame; the caller signal applies while the resolver request is pending.
