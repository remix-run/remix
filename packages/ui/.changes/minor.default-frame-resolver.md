`run()` now uses a default browser frame resolver when `resolveFrame` is omitted. The default fetches the frame source with `Accept: text/html`, forwards the submitted method and `FormData`, and supports cancellation with the provided abort signal. Apps only need a custom resolver for behavior such as additional request headers, alternate body encoding, redirects, or custom error presentation.
