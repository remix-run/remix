Allow `resolveClientEntry()` to return import maps for hydrated client entry module graphs. During server rendering, discovered mappings are emitted in a framework-owned import map without modifying authored import maps. Later frame responses transport new mappings in a leading `<head>`, and the client runtime deduplicates and installs them before hydrating newly discovered client entries.
