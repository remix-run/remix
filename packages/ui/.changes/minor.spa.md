Export an `SPA` component that renders same-origin browser navigations through a URL-to-`RemixNode` router, exposes active and pending URLs through component context, forwards cancellation signals, and dispatches intercepted form submissions with their `FormData`.
